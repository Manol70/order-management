{% extends '@EasyAdmin/layout.html.twig' %}

{% block page_content %}
    <div class="custom-logo" style="text-align: center;">
        <img src="{{ asset('images/logo.png') }}" alt="Logo" style="width: 200px; margin-bottom: 20px;">
    </div>

    {{ parent() }} {# Оригиналното съдържание на страницата #}
{% endblock %}

{% block user_menu %}
    <div class="custom-greeting">
        <h2>Welcome, !</h2>
    </div>
    {{ parent() }}
{% endblock %}